import * as React from 'react';
import { Page } from '../../components/Page';

const List = () => {
    return (
        <Page title='Listagem de categorias'>
            Minhas categorias
        </Page>
    );
};

export default List;